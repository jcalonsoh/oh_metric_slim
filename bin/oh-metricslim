#!/usr/bin/env ruby
# Encoding: utf-8

Signal.trap('INT') { exit 1 }

$LOAD_PATH.unshift File.join(File.dirname(__FILE__), %w(.. lib))
require 'rubygems'
require 'oh_metric_slim/cli'

OhMetricSlim::CLI.start
